/// This creates a rope; now to just get this to work by connecting it with the joint_rope_create
/*
if(!parent != -1)
{
	draw_line_width(x, y, parent.x, parent.y, 3);
}	
*/

//physics_draw_debug();
